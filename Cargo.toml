[workspace]
members = ["crates/dcg-core", "crates/dcg-cli", "fuzz"]
resolver = "2"
default-members = ["crates/dcg-core", "crates/dcg-cli"]

# Workspace-level shared profiles. Cargo applies these to every member
# crate; per-crate profile overrides are silently ignored, so they live
# at the workspace root.

[profile.release]
opt-level = "z"     # Optimize for size (lean binary for distribution)
lto = true          # Link-time optimization across crates
codegen-units = 1   # Single codegen unit for better optimization
panic = "abort"     # Smaller binary, no unwinding overhead
strip = true        # Remove debug symbols

[profile.dev]
opt-level = 1       # Faster compile times during development
